[package]
name = "debugger"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
leptos = { path = "../../leptos", features = ["csr", "nightly", "debugger"] }
console_log = "1"
log = "0.4"
console_error_panic_hook = "0.1.7"