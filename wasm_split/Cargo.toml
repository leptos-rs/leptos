[package]
name = "wasm_split"
version = "0.1.0"
edition = "2021"

[dependencies]
async-once-cell = { default-features = true, workspace = true, features = [
	"std",
] }
wasm_split_macros.workspace = true
